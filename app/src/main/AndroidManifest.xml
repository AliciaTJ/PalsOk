<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="e.alicia.pals">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Ayuda"></activity>
        <activity android:name=".GestionBD" />
        <activity android:name=".PublicarNoticia" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-6028655171173553~5354375321" />

        <activity android:name=".MisPlanes" />
        <activity android:name=".Opciones" />
        <activity android:name=".Chat" />
        <activity android:name=".VerPerfil" />
        <activity android:name=".Registrar" />
        <activity android:name=".VerPlan" />
        <activity android:name=".PlanNuevo" />
        <activity
            android:name=".TipoPlan"
            android:label="@string/tipo" />
        <activity
            android:name=".Planes"
            android:label="@string/buscarPlanes" />
        <activity
            android:name=".ActivityPortada"
            android:label="@string/titulo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>